[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python app.py"
healthCheckPath = "/"
healthCheckTimeout = 30

[[services]]
port = 8000
protocol = "tcp"
